# 飞书网页保存Chrome插件 - 故障排除指南

## 常见问题和解决方案

### 🔐 权限问题

#### 问题：写入权限检查失败 / Permission denied (403)

**症状：**
- 点击"🔐 检查权限"显示"写入权限检查失败"
- 保存到飞书时提示"Permission denied"

**解决方案：**
1. **关闭飞书高级权限**（已验证有效）
   - 打开多维表格
   - 点击右上角"高级权限"
   - 关闭高级权限功能

2. **检查应用权限配置**
   - 在飞书开放平台确保有以下权限：
     - `bitable:app` ✅
     - `bitable:app:readonly` ✅
     - `base:record:retrieve` ✅

3. **确保应用已发布**
   - 应用必须处于"已发布"状态
   - 开发状态的应用权限有限

4. **重新获取访问令牌**
   - 权限配置后重新生成 `app_access_token`
   - Token有效期2小时，过期需重新获取

### 📋 字段配置

#### 问题：字段不匹配导致写入失败

**必需字段：**
- 标题（单行文本）
- 链接（单行文本）
- 备注（多行文本，可选）
- 标签（单行文本，可选）
- 保存时间（单行文本或日期）

**注意：**
- 字段名称必须完全匹配，区分大小写
- 建议使用单行文本类型避免格式问题

### 🔗 URL解析

#### 问题：无法解析飞书表格链接

**支持格式：**
```
https://xxx.feishu.cn/base/DeI4by0Thac5m4sGzSJcwRt5nVb?table=tblNg6iVyvBIF2FM
```

**解决方案：**
1. 确保链接包含 `?table=` 参数
2. 使用"🔍 测试解析"功能验证URL格式

### 🚀 快速排查步骤

1. **检查表格高级权限状态**
2. **验证应用发布状态**
3. **重新配置访问令牌**
4. **使用"🔐 检查权限"功能诊断**

---

## ✅ 成功案例

关闭飞书高级权限后，插件功能完全正常工作。这是最常见的权限问题解决方案。